(function (def) {

  /**
   * defines widget to be used for the common
   */
  def.hash({
    widget: [
      'app.admin.dispute.widget.List'
    ]
  }).as('app.admin.dispute.mapping');

}(require_joo()));
