(function (def) {

  /**
   * defines widget to be used for the common
   */
  def.hash({
    widget: [
      'app.user_manage.widget.List'
    ]
  }).as('app.user_manage.mapping');

}(require_joo()));
