(function (def) {

  /**
   * defines widget to be used for the common
   */
  def.hash({
    widget: [
      'app.admin_default.widget.BankLog'
    ]
  }).as('app.admin_default.mapping');

}(require_joo()));
