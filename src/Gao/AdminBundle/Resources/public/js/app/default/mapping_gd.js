(function (def) {
  /**
   * define the widget to be managed by the ra_detail
   */
  def.hash({
    widget: [
      'app.default.widget.Gd'
    ]
  }).as('app.default.mapping_gd');

}(require_joo()));
