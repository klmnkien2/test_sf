(function (def) {
  /**
   * define the widget to be managed by the ra_detail
   */
  def.hash({
    widget: [
      'app.default.widget.Pd'
    ]
  }).as('app.default.mapping_pd');

}(require_joo()));
