(function (def) {
  /**
   * define the widget to be managed by the ra_detail
   */
  def.hash({
    widget: [
      'app.default.widget.UploadImage'
    ]
  }).as('app.default.mapping_dispute');

}(require_joo()));
