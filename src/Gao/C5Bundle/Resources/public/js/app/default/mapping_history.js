(function (def) {
  /**
   * define the widget to be managed by the ra_detail
   */
  def.hash({
    widget: [
      'app.default.widget.History'
    ]
  }).as('app.default.mapping_history');

}(require_joo()));
